import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';
// to render application to the browser we need ReactDOM.render()
ReactDOM.render(<App/>, document.getElementById("root"));